import { expect } from 'chai';
import 'mocha';

describe('Autoinstall', () => {
    it('Mocha sanity check', () => {
        expect(true).true;
    });

    it('Install system tables', async () => {

    });
});
